<%= form_with(model: rich_text_test) do |form| %>
  <% if rich_text_test.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(rich_text_test.errors.count, "error") %> prohibited this rich_text_test from being saved:</h2>

      <ul>
        <% rich_text_test.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
